export type loginBody = {
    username : string;
    password : string;
}

export type userTypeId = {
    id : number;
    first_name : string;
    last_name : string;
    username: string;
    password : string;
}

export type userType = {
    first_name : string;
    last_name : string;
    password : string;
    
}
